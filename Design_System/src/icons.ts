/**
 * Public entry point: iconography.
 * `Icon` renders a vendored Lucide glyph (see assets/icons/) via CSS mask + currentColor;
 * `DOMAIN_ICON_MAP` is the canonical domain-concept -> glyph mapping (icons/icon-map.md).
 */
export * from "../components/icons/Icon";
