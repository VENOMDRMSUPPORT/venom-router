Diagnostics set: TypedErrorDisplay (code + user-safe message + retryable), TraceId, DiagnosticEventRow, EvidenceList (provenance + confidence + identity match), AuditEventTimeline, JobStatus/JobProgress (the five shared job states), RawPayloadDisclosure (explicit redaction note).

```jsx
<TypedErrorDisplay code="venom_no_eligible_offering" retryable retryAfter="41s" requestId="req_01J9ZK4T7Q" message="No certified, funded, capable route is currently available for venom/lite." />
```

Never vague copy ("Something went wrong"); never raw provider errors.
