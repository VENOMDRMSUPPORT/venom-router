Standard action button. One primary per view; danger for destructive actions (pair with DestructiveActionConfirmation for irreversible ones).

```jsx
<Button variant="primary" icon="plus">Connect provider</Button>
<Button variant="danger" loading>Disconnecting...</Button>
```
