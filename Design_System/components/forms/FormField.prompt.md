Label + control + help + error with automatic ARIA wiring. Every form row uses this.

```jsx
<FormField label="API key" description="Validated with a zero-cost probe." error={err}><Input mono /></FormField>
```
