Theme-aware icon from the pinned Lucide set; accepts domain concept names (quota, certification, oauth...) or raw glyph names.

```jsx
<Icon name="certification" />
<Icon name="quota" size={20} label="Quota" />
```

Decorative unless `label` is given. Never use an icon alone for critical state - pair with text.
