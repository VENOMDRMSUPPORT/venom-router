Use for a real navigation destination whose implementation is intentionally absent. Supply the destination's real title and description. Do not use this for an empty data set; use `EmptyState` for that case.

```tsx
<PlannedSurface icon="activity" title="Diagnostics" description="Health checks and routing failure inspection." />
```
