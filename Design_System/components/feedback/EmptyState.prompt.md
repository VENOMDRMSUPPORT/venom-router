Empty view with copy + action.

```jsx
<EmptyState icon="server" title="No providers connected" description="Connect your first provider account to start discovering models." action={<Button variant="primary">Connect provider</Button>} />
```
